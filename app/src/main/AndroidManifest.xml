<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vendecar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Insertar_Activity"></activity>
        <activity android:name=".Ficha_Activity"></activity>
        <activity android:name=".Eliminar_Activity" />
        <activity
            android:name=".Preferncias_Activity"
            android:label="@string/title_activity_preferncias_" />
        <activity android:name=".Listado_Activity" />
        <activity android:name=".Vendecar_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>